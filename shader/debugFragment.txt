#version 330 core
out vec4 FragColor;

in vec2 TexCoords;

uniform sampler2D scene;

void main()
{
    float color = texture(scene, TexCoords).r;
    FragColor = vec4(color, color, color, 1.0);             
    //FragColor = texture(scene, TexCoords);
}